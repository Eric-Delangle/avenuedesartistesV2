{% extends 'base.html.twig' %}

{% block title %}GalleryVente{% endblock %}

{% block body %}
    <h1>GalleryVente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gallery_vente.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gallery_vente.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ gallery_vente.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gallery_vente_index') }}">back to list</a>

    <a href="{{ path('gallery_vente_edit', {'id': gallery_vente.id}) }}">edit</a>

    {{ include('gallery_vente/_delete_form.html.twig') }}
{% endblock %}
