{% extends 'base.html.twig' %}

{% block title %}User index
{% endblock %}

{% block body %}
	<h1>User index</h1>

	<table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Email</th>
				<th>Password</th>
				<th>FirstName</th>
				<th>LastName</th>
				<th>Slug</th>
				<th>Location</th>
				<th>Avatar</th>
				<th>RegisteredAt</th>
				<th>Niveau</th>
				<th>Description2</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for user in users %}
				<tr>
					<td>{{ user.id }}</td>
					<td>{{ user.email }}</td>
					<td>{{ user.password }}</td>
					<td>{{ user.firstName }}</td>
					<td>{{ user.lastName }}</td>
					<td>{{ user.slug }}</td>
					<td>{{ user.location }}</td>
					<td>{{ user.avatar }}</td>
					<td>{{ user.registeredAt ? user.registeredAt|date('Y-m-d H:i:s') : '' }}</td>
					<td>{{ user.niveau }}</td>
					<td>{{ user.description2 }}</td>
					<td>
						<a href="{{ path('user_show', {'slug': user.slug}) }}">show</a>
						<a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="12">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	<a href="{{ path('user_new') }}">Create new</a>
{% endblock %}
