<form method="post" action="{{ path('gallery_echange_delete', {'id': gallery_echange.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gallery_echange.id) }}">
	<button class="btn bouton mt-2">Supprimer</button>
</form>
