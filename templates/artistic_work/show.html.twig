{% extends 'base.html.twig' %}

{% block title %}ArtisticWorkEchange{% endblock %}

{% block body %}
    <h1>ArtisticWorkEchange</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artistic_work.id }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ artistic_work.slug }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artistic_work.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ artistic_work.createdAt ? artistic_work.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ artistic_work.picture }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ artistic_work.updatedAt ? artistic_work.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ artistic_work.description }}</td>
            </tr>
        </tbody>
    </table>

 {#   <a href="{{ path('artistic_work') }}">back to list</a> #}

    <a href="{{ path('artistic_work_edit', {'id': artistic_work.id}) }}">edit</a>

    {{ include('artistic_work/_delete_form.html.twig') }}
{% endblock %}
